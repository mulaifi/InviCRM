# InviCRM Production Environment Variables
# Copy to .env.prod and fill in real values

# ===========================================
# Domain Configuration
# ===========================================
DOMAIN=invicrm.example.com
# Services will be available at:
# - api.invicrm.example.com (Core API)
# - slack.invicrm.example.com (Slack bot webhooks)
# - traefik.invicrm.example.com (Dashboard - optional)

# ===========================================
# SSL/TLS (Let's Encrypt)
# ===========================================
ACME_EMAIL=admin@example.com

# ===========================================
# Traefik Dashboard Auth (optional)
# ===========================================
# Generate with: htpasswd -nb admin yourpassword
TRAEFIK_AUTH=admin:$apr1$xxx$yyy

# ===========================================
# Database
# ===========================================
DB_USER=invicrm
DB_PASSWORD=CHANGE_ME_STRONG_PASSWORD
DB_NAME=invicrm

# ===========================================
# Redis
# ===========================================
REDIS_PASSWORD=CHANGE_ME_STRONG_PASSWORD

# ===========================================
# Authentication
# ===========================================
JWT_SECRET=CHANGE_ME_LONG_RANDOM_STRING_MIN_32_CHARS
JWT_EXPIRES_IN=7d

# ===========================================
# Google OAuth (from console.cloud.google.com)
# ===========================================
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret

# ===========================================
# Slack App (from api.slack.com/apps)
# ===========================================
SLACK_CLIENT_ID=your-slack-client-id
SLACK_CLIENT_SECRET=your-slack-client-secret
SLACK_SIGNING_SECRET=your-signing-secret
# App-level token for Socket Mode (starts with xapp-)
SLACK_APP_TOKEN=xapp-your-app-token

# ===========================================
# Anthropic AI (from console.anthropic.com)
# ===========================================
ANTHROPIC_API_KEY=sk-ant-your-api-key
